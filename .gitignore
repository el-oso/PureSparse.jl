# Julia
Manifest.toml
*.jl.cov
*.jl.*.cov
*.jl.mem

# Compiled artifacts (juliac --trim output)
*.so
*.dylib
*.dll
juliac/build/

# Documenter / DocumenterVitepress / Node
docs/build/
**/.documenter/
node_modules/
package-lock.json
.vitepress/cache/
.vitepress/dist/

.DS_Store

# Per-host plot data cache (regenerated by bench/plots.jl)
bench/plots_data_*.txt

# Claude Code local session/plan/worktree directory (not part of the package)
.claude/

# Local clean-room reference archive (papers only; not distributed with the package)
refs/

# Per-host benchmark results (regenerated by benchmark/gate.jl; PureBLAS's own convention
# for bench/plots_data_*.txt is not to commit per-host measurement caches)
benchmark/results/
benchmark/LocalPreferences.toml
