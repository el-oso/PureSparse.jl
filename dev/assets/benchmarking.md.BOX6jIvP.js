import{_ as t,o as a,c as s,ao as r}from"./chunks/framework.D4wlFlaD.js";const i="/PureSparse.jl/dev/assets/qr_gate_strata.SEchJCAY.png",o="/PureSparse.jl/dev/assets/qr_faer_comparison.CHwaSXdW.png",m=JSON.parse('{"title":"Benchmarking","description":"","frontmatter":{},"headers":[],"relativePath":"benchmarking.md","filePath":"benchmarking.md","lastUpdated":null}'),n={name:"benchmarking.md"};function l(d,e,h,c,p,g){return a(),s("div",null,[...e[0]||(e[0]=[r(`<h1 id="Benchmarking" tabindex="-1">Benchmarking <a class="header-anchor" href="#Benchmarking" aria-label="Permalink to &quot;Benchmarking {#Benchmarking}&quot;">​</a></h1><h2 id="Methodology" tabindex="-1">Methodology <a class="header-anchor" href="#Methodology" aria-label="Permalink to &quot;Methodology {#Methodology}&quot;">​</a></h2><p><code>benchmark/gate.jl</code> runs the M1 wall-time gate: <a href="https://github.com/LilithHafner/Chairmarks.jl" target="_blank" rel="noreferrer">Chairmarks.jl</a> medians (not min), single-thread pinned (<code>BLAS.set_num_threads(1)</code>), <code>evals=1</code> (each sample is a fresh, independent timed call — appropriate since factorization refactor is cheap enough per-call that batching isn&#39;t needed at the matrix sizes tested), a sample budget capped at 30 samples / 1.5s per measurement. <code>benchmark/benchmarks.jl</code> is a separate <a href="https://github.com/JuliaCI/PkgBenchmark.jl" target="_blank" rel="noreferrer">PkgBenchmark.jl</a> suite for commit-to-commit self-regression (<code>judge(PureSparse, &quot;HEAD&quot;, &quot;base&quot;)</code>) — it answers &quot;did my change make my code slower?&quot;, which the gate does not.</p><p>For a methodologically-valid (clock-locked) run, lock CPU frequency first — this repo doesn&#39;t duplicate a locking script; PureBLAS.jl&#39;s <code>bench/fleet_freqlock.sh lock</code> covers the same machine. An unlocked run still produces real measured numbers, just noisier.</p><h2 id="Configurations" tabindex="-1">Configurations <a class="header-anchor" href="#Configurations" aria-label="Permalink to &quot;Configurations {#Configurations}&quot;">​</a></h2><p>Three of the design&#39;s four configurations (<code>docs/design.md</code> §9.3) are measured — the fourth, CHOLMOD+PureBLAS, is <strong>N/A</strong>, blocked on PureBLAS&#39;s documented <code>BLAS.lbt_forward</code>-from-a-live-Julia-process limitation (see PureBLAS.jl&#39;s docs): 2. <strong>PureSparse + PureBLAS</strong> (primary) — the actual shipped stack.</p><ol start="3"><li><p><strong>PureSparse + OpenBLAS</strong> (kernel-attribution arm) — <code>benchmark/openblas_backend.jl</code> re-<code>include</code>s <code>src/numeric/llt.jl</code> verbatim under a different kernel binding (OpenBLAS via <code>LinearAlgebra.LAPACK</code>/<code>BLAS</code> instead of PureBLAS), so this isolates PureBLAS-vs-OpenBLAS kernel efficiency from the sparse scheduling layer — no algorithm duplication, same source file, different <code>using</code>.</p></li><li><p><strong>CHOLMOD (SparseArrays) + OpenBLAS</strong> (baseline).</p></li></ol><p>Both <strong>own-ordering</strong> (each stack&#39;s own AMD) and <strong>same-permutation</strong> (each stack fed the <em>other&#39;s</em> chosen permutation via <code>GivenOrdering</code>/<code>perm=</code>) arms run — the latter isolates factorization throughput from ordering quality and is part of the gate, not supplementary.</p><h2 id="Current-result" tabindex="-1">Current result <a class="header-anchor" href="#Current-result" aria-label="Permalink to &quot;Current result {#Current-result}&quot;">​</a></h2><p>As of 2026-07-13 on <code>neuromancer</code> (NOT clock-locked — see caveat above), <strong>6/14</strong> matrix-arm combinations beat CHOLMOD+OpenBLAS on warm numeric refactor. This does <strong>not</strong> yet meet M1&#39;s gate (&quot;strictly faster on at least half the set&quot;). The root cause has been diagnosed (not guessed — measured): it is not an ordering-quality gap (PureSparse&#39;s AMD fill matches or beats CHOLMOD&#39;s on every failing case) but a relaxed-amalgamation limitation that under-merges supernodes on bushy elimination trees (2D grid Laplacians being the clearest failing case). See <code>ROADMAP.md</code>&#39;s &quot;CURRENT FOCUS&quot; section for the full table and diagnosis — that file is the living source of truth for gate status; this page won&#39;t be kept in perfect sync with every run.</p><h2 id="Reproducing" tabindex="-1">Reproducing <a class="header-anchor" href="#Reproducing" aria-label="Permalink to &quot;Reproducing {#Reproducing}&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=benchmark</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> benchmark/gate.jl</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # measure + save + print gate verdict</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=benchmark</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> benchmark/gate.jl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> report</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # print verdict from the last saved JSON only</span></span></code></pre></div><p>Results are written to <code>benchmark/results/gate_&lt;hostname&gt;.json</code> (gitignored — per-host measurement caches aren&#39;t committed, matching PureBLAS.jl&#39;s convention for its own per-host benchmark data).</p><h2 id="Sparse-QR-M5" tabindex="-1">Sparse QR (M5) <a class="header-anchor" href="#Sparse-QR-M5" aria-label="Permalink to &quot;Sparse QR (M5) {#Sparse-QR-M5}&quot;">​</a></h2><h3 id="Methodology-2" tabindex="-1">Methodology <a class="header-anchor" href="#Methodology-2" aria-label="Permalink to &quot;Methodology {#Methodology-2}&quot;">​</a></h3><p><code>benchmark/qr_gate.jl</code> runs the M5 wall-time gate with the same discipline as M1&#39;s: Chairmarks medians, single-thread pinned, <code>evals=1</code>, 20 samples / 1.5s per measurement. The gate is <strong>cold-vs-cold only</strong> — one-shot <code>qr(A)</code> including symbolic analysis — because stdlib SuiteSparseQR [<a href="/PureSparse.jl/dev/references#spqr2011">1</a>] exposes no analyze-once/refactorize path to compare warm <code>qr!</code> against (warm numbers are reported in the JSON, not gated). Both <strong>own-ordering</strong> (PureSparse&#39;s COLAMD vs SPQR&#39;s default) and <strong>same-permutation</strong> arms run, as for Cholesky. PureSparse&#39;s number is the <strong>best of <code>:column</code>/<code>:frontal</code></strong> per matrix-arm — the same choice <code>qr(A; method = :auto)</code> makes for a real caller.</p><p>The gate set is stratified into three regimes (design_qr.md §9.3), and the M5 closeout gate requires <strong>every stratum to pass, both arms</strong> — not just a majority:</p><ul><li><p><code>i_singleton</code> — LP-shaped, singleton-dominated matrices (<code>lp_slack</code>, <code>staircase</code>).</p></li><li><p><code>ii_sparse_R</code> — genuinely sparse R, little dense work (<code>banded_ls</code>, <code>grid_ls</code>).</p></li><li><p><code>iii_flop_rich</code> — dense-panel-heavy problems where BLAS-3 fronts pay (<code>dense_arrow</code>, <code>random_tall</code>).</p></li></ul><p>Two context arms are measured alongside but are <strong>not</strong> part of the pass/fail verdict: PureSparse&#39;s own <code>cholesky(AᵀA)</code> normal equations (the §1.2 &quot;when not to use QR&quot; alternative) and [<a href="/PureSparse.jl/dev/references#faer">2</a>]&#39;s sparse QR via a <code>ccall</code> shim (its ordering/threshold choices differ, so gating on it would conflate ordering quality with kernel throughput).</p><h3 id="Current-result-2" tabindex="-1">Current result <a class="header-anchor" href="#Current-result-2" aria-label="Permalink to &quot;Current result {#Current-result-2}&quot;">​</a></h3><p>As of 2026-07-15 on <code>galen</code> (clock-locked, <code>performance</code> governor), <strong>6/16</strong> matrix-arm combinations beat SuiteSparseQR cold. This does <strong>not</strong> yet meet M5&#39;s gate. Per stratum:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Stratum</th><th style="text-align:right;">Passing</th><th style="text-align:right;">Where it stands</th></tr></thead><tbody><tr><td style="text-align:right;"><code>iii_flop_rich</code></td><td style="text-align:right;"><strong>4/4</strong></td><td style="text-align:right;">clean sweep — the multifrontal path (M5b) wins every flop-rich case, by up to ~2.5×</td></tr><tr><td style="text-align:right;"><code>i_singleton</code></td><td style="text-align:right;">2/6</td><td style="text-align:right;">losses are noise-level margins (~0.04–0.15 ms either way); <code>:auto</code> already routes these to <code>:column</code>, and the residual gap is fixed per-call setup overhead, not an algorithmic deficit</td></tr><tr><td style="text-align:right;"><code>ii_sparse_R</code></td><td style="text-align:right;">0/6</td><td style="text-align:right;">the real open front: SPQR stays ~1.1–2.3× ahead on sparse-R problems (<code>banded_ls</code> own-arm ≈ 2.2× is the worst); <code>grid_ls_70x50</code>&#39;s single-sample gate readings additionally have high measured sample-to-sample variance, so no single verdict there is trusted without a longer run</td></tr></tbody></table><p><img src="`+i+'" alt=""></p><p><code>ROADMAP.md</code> is the living source of truth for the diagnosis trail (amalgamation retuning tested and ruled out; panel-split trigger bug found and fixed — that fix is what closed <code>iii_flop_rich</code>; scalar small-front fallback landed); this page won&#39;t be kept in perfect sync with every run.</p><h3 id="The-flagship-dense-panel-case-70004000" tabindex="-1">The flagship dense-panel case (7000×4000) <a class="header-anchor" href="#The-flagship-dense-panel-case-70004000" aria-label="Permalink to &quot;The flagship dense-panel case (7000×4000) {#The-flagship-dense-panel-case-70004000}&quot;">​</a></h3><p>Where the multifrontal path&#39;s BLAS-3 architecture is actually exercised — a 7000×4000 random matrix at 1% and 10% density (<code>benchmark/faer_vs_puresparse_7000x4000.jl</code>) — PureSparse&#39;s <code>:frontal</code> path beats both [<a href="/PureSparse.jl/dev/references#faer">2</a>] and SuiteSparseQR outright (galen, clock-locked, cold medians of 10 samples; four runs across two comparator implementations now agree within ~1%):</p><table tabindex="0"><thead><tr><th style="text-align:right;">density</th><th style="text-align:right;">PureSparse <code>:frontal</code></th><th style="text-align:right;">faer</th><th style="text-align:right;">SuiteSparseQR</th><th style="text-align:right;">vs faer</th><th style="text-align:right;">vs SPQR</th></tr></thead><tbody><tr><td style="text-align:right;">1%</td><td style="text-align:right;"><strong>1.72 s</strong></td><td style="text-align:right;">4.00 s</td><td style="text-align:right;">5.23 s</td><td style="text-align:right;">2.3×</td><td style="text-align:right;">3.0×</td></tr><tr><td style="text-align:right;">10%</td><td style="text-align:right;"><strong>0.89 s</strong></td><td style="text-align:right;">4.23 s</td><td style="text-align:right;">5.74 s</td><td style="text-align:right;">4.8×</td><td style="text-align:right;">6.5×</td></tr></tbody></table><p><img src="'+o+`" alt=""></p><p><strong>Comparator fairness check (2026-07-15):</strong> the [<a href="/PureSparse.jl/dev/references#faer">2</a>] side originally timed factorize <em>and</em> a least-squares solve together (<code>BlazingPorts.jl</code>&#39;s shim needed a solve as a compiler dead-code-elimination guard, since faer&#39;s sparse <code>Qr</code> exposes no direct <code>.R()</code> accessor) — not a fair comparison against PureSparse&#39;s/SPQR&#39;s factorize-only wrappers. Fixed by adding <code>faer_sparse_qr_factor</code> (<code>std::hint::black_box</code> as the guard instead) and re-measuring: the corrected numbers above are within ~0.7% of the pre-fix ones, confirming the solve was cheap relative to the factorization and the original margin wasn&#39;t a measurement artifact — but the fix is real and the comparator now measures the same thing on both sides.</p><p>(The <code>:column</code> path takes ~60 s here — this is exactly the regime <code>method = :auto</code> exists to route away from.) The honest overall picture: PureSparse QR is already the fastest of the three at the large, flop-rich end, and not yet competitive with SPQR on small singleton-heavy and sparse-R problems — which is why the gate stays open.</p><h3 id="Reproducing-2" tabindex="-1">Reproducing <a class="header-anchor" href="#Reproducing-2" aria-label="Permalink to &quot;Reproducing {#Reproducing-2}&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=benchmark</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> benchmark/qr_gate.jl</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # measure + save + print gate verdict</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=benchmark</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> benchmark/qr_gate.jl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> report</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # verdict from the last saved JSON only</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=benchmark</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> benchmark/plot_qr_comparison.jl</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # regenerate the two plots above</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                                           # from the SAVED JSON (never re-measures)</span></span></code></pre></div><p>The plots regenerate from <code>benchmark/results/qr_gate_galen.json</code> and <code>benchmark/results/faer_vs_puresparse_7000x4000_galen.json</code> — saved measurement snapshots; re-running a benchmark to make a plot is against this repo&#39;s benchmarking rules (results→JSON first, plots from saved JSON only).</p>`,33)])])}const k=t(n,[["render",l]]);export{m as __pageData,k as default};
